# ─── 依赖 / 构建产物 ─────────────────────────────────────
node_modules/
dist/
dist-ssr/
vendor/

# ─── 环境变量 ────────────────────────────────────────────
# 后端 .env 含数据库密码 / JWT 密钥，禁止提交（保留 .env.example 作模板）
/backend/.env
# 本地覆盖文件一律不提交
.env.local
.env.*.local
*.local

# ─── 运行时 / 日志 / 缓存 ────────────────────────────────
runtime/
*.log
.phpunit.result.cache

# ─── 自动生成 ────────────────────────────────────────────
src/types/import/auto-imports.d.ts
src/types/import/components.d.ts
.auto-import.json

# ─── 上传目录（运行时数据） ──────────────────────────────
public/uploads/

# ─── IDE / 编辑器 ────────────────────────────────────────
.idea/
.vscode/
.cursorrules

# ─── AI 助手本地配置 ─────────────────────────────────────
.claude/

# ─── OS ──────────────────────────────────────────────────
.DS_Store
Thumbs.db
